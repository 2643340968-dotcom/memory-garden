# Grass assets

The first version uses procedural instanced planes from `src/scene/createGrass.js` and does not require an external grass texture. This directory is reserved for later transparent grass cards.

