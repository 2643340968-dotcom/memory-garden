# 记忆花园 / Memory Garden
## ChatGPT × Codex 项目聊天整理

> 本文档整理自本次关于“紫金草 / 记忆花园”网页交互项目的完整讨论，目的是方便后续在 Codex、另一台电脑或其他账号中继续开发。
> 内容包含：项目概念、视觉方向、技术路线、素材规范、交互逻辑、Codex Skills、各阶段 Prompt 目标、当前状态、迁移方式与下一步计划。

---

# 1. 项目最终概念

## 1.1 项目名称

**中文：记忆花园**  
**英文：Memory Garden**

页面顶部最终建议：

- `INTERACTIVE MEMORY GARDEN`
- `记忆花园`
- `MEMORY BLOOMS`

## 1.2 核心主题

项目以 **南京大屠杀 / 江东门** 为背景，通过“紫金草”作为记忆的象征。

核心隐喻：

> **一朵花 = 一段个人记忆**

观众进入网页后，不只是“种花”，而是在参与一个由个人记忆不断组成的数字花园。

## 1.3 完整交互逻辑

```text
进入网页
↓
出现记忆留言弹窗
↓
观众输入一段与南京大屠杀 / 江东门 / 自身记忆有关的文字
↓
提交
↓
弹窗淡出
↓
用户自己的第一段记忆自动触发一丛紫金草 BloomEvent
↓
该记忆短暂浮现在花丛附近
↓
MOUSE INPUT · READY
↓
观众按住鼠标拖动
↓
花丛一丛一丛突然生长
↓
拖动过程中出现 1–3 个 Memory Echo
↓
Memory Echo 以毛玻璃 / 发光记忆碎片形式漂浮在花丛附近
↓
Memory Echo 淡出
↓
观众继续种植
↓
整个草地逐渐成为由“记忆”组成的花园
```

未来版本：

```text
MouseInput
↓
替换为 MediaPipe HandInput
↓
手势种花
```

同时保留鼠标作为 fallback。

---

# 2. 最初参考与视觉目标

最初想实现的效果参考：

- 手 / 鼠标在空间中移动
- 花不是沿一条线被“拉出来”
- 而是某个区域被激活后：
  - 一整丛花突然从地面膨出
  - 多个 BloomEvent 叠加
  - 最终长成花田

目标感觉：

> **“啪——这一块突然长出来了”**

而不是：

> 鼠标像笔一样沿路径画一条花线。

---

# 3. 技术路线演化

## 3.1 最早讨论：3D 模型方案

最初考虑：

- Three.js
- MediaPipe Hands
- GLB 紫金草模型
- InstancedMesh
- 草地程序化生成

建议流程：

```text
Mouse
↓
PointerController
↓
GroundRaycaster
↓
BloomEvent
↓
FlowerSystem
```

未来替换：

```text
HandInput
↓
PointerController
↓
GroundRaycaster
↓
BloomEvent
```

核心原则：

> 不要把 MouseInput 和 FlowerSystem 写死在一起。

## 3.2 为什么先鼠标后手势

开发顺序确定为：

1. 鼠标交互
2. 跑通 Raycaster
3. 跑通 BloomEvent
4. 正式紫金草视觉
5. 最后再接 MediaPipe

原因：

- 鼠标输入更容易调试
- 可以先把视觉效果做好
- 后续只替换输入源
- 不需要推翻种花系统
- 鼠标还能保留作为摄像头权限失败时的 fallback

---

# 4. 最初 Codex 项目架构

推荐结构：

```text
src/

main.js
config.js

scene/
    createScene.js
    createCamera.js
    createLights.js
    createGround.js
    createGrass.js

input/
    PointerController.js
    MouseInput.js

interaction/
    GroundRaycaster.js

flowers/
    FlowerSystem.js
    FlowerSpawner.js
    FlowerAnimation.js

utils/
    random.js
```

核心架构：

```text
MouseInput / Future HandInput
↓
PointerController
↓
GroundRaycaster
↓
BloomEvent
↓
Flower Renderer
```

---

# 5. Codex Skills 使用规则

用户已安装的相关 Skills 包括：

## 通用设计

- Frontend Design
- Humanize
- Mobile Design
- Generate Figma Design
- Figma Implement Design
- Create Design System Rules
- Mobile Screenshot to Figma

## Three.js Skills

- Threejs Animation
- Threejs Audio
- Threejs Camera
- Threejs Controls
- Threejs Dev Setup
- Threejs Geometries
- Threejs Helpers
- Threejs Lights
- Threejs Loaders
- Threejs Materials
- Threejs Math
- Threejs Node Tsl
- Threejs Objects
- Threejs Postprocessing
- Threejs Renderers
- Threejs Scenes
- Threejs Textures
- Threejs Webxr

## 5.1 重要约定

后续每次给 Codex Prompt 时，都要同时说明：

> **本次需要勾选哪些 Skills**

避免重复询问。

## 5.2 第一版核心交互常用 Skills

- Threejs Dev Setup
- Threejs Scenes
- Threejs Camera
- Threejs Renderers
- Threejs Objects
- Threejs Math
- Threejs Animation
- Threejs Geometries
- Threejs Materials
- Frontend Design

## 5.3 花丛分布算法阶段

主要：

- Threejs Math
- Threejs Objects
- Threejs Animation

可选：

- Threejs Geometries

## 5.4 PNG 花卡阶段

主要：

- Threejs Textures
- Threejs Materials
- Threejs Objects
- Threejs Animation
- Threejs Geometries
- Threejs Math
- Threejs Renderers

## 5.5 暗色氛围阶段

- Threejs Materials
- Threejs Lights
- Threejs Renderers
- Frontend Design
- Threejs Postprocessing

## 5.6 Memory Echo UI 阶段

- Frontend Design
- Threejs Animation
- Threejs Math
- Threejs Objects

---

# 6. 鼠标种花系统的关键迭代

## 6.1 最初问题：路径太线性

最初效果：

```text
鼠标路径
──────────────→
🌸 🌸 🌸 🌸 🌸
```

问题：

- 花沿着路径生成
- 像“拉出一串花”
- 缺少一丛一丛爆开的感觉

## 6.2 第一轮解决：Patch / Cluster

将：

```text
path point
→ spawn flowers
```

改为：

```text
path point
→ Flower Patch
→ 2–4 sub-clusters
→ 14–28 flowers
```

关键参数：

```text
PATCH_SPAWN_SPACING
PATCH_RADIUS_MIN
PATCH_RADIUS_MAX
FLOWERS_PER_PATCH_MIN
FLOWERS_PER_PATCH_MAX
SUBCLUSTER_MIN
SUBCLUSTER_MAX
SUBCLUSTER_OFFSET_MIN
SUBCLUSTER_OFFSET_MAX
PATH_LATERAL_JITTER
EDGE_REJECTION_RATE
PATCH_ELLIPSE_MIN
PATCH_ELLIPSE_MAX
```

## 6.3 横向拖动仍然线性

原因：

- 世界坐标 X/Z 分布受透视影响
- 水平拖动时扩散可能主要落在 Z 深度方向
- 从屏幕看仍像一条线

解决思路：

> **Screen-space Brush → Raycast to Ground**

流程：

```text
鼠标屏幕位置
↓
在屏幕像素范围内随机采样
↓
转换为 NDC
↓
Raycast 到 Ground
↓
得到 3D 花位置
```

重要参数：

```text
BRUSH_RADIUS_PX_MIN
BRUSH_RADIUS_PX_MAX
BRUSH_SUBCLUSTER_MIN
BRUSH_SUBCLUSTER_MAX
BRUSH_EDGE_REJECTION
BRUSH_DIRECTION_STRETCH
```

---

# 7. 真正关键的算法：BloomEvent

最后确定：

> 问题不是花丛“宽不宽”，而是交互模型不应该是 continuous stroke。

最终机制：

```text
Mouse movement
↓
判断距离 / cooldown
↓
createBloom(anchorPosition)
↓
一次 BloomEvent 负责一整片花
↓
BloomEvent 独立完成膨胀动画
```

## 7.1 BloomEvent 数据

建议包含：

```text
anchorPosition
startTime
duration
radius
flowerCount
randomSeed
subClusterCenters
```

## 7.2 Bloom 触发

建议：

```text
BLOOM_TRIGGER_DISTANCE ≈ 0.7–1.2
BLOOM_TRIGGER_COOLDOWN ≈ 120–220ms
```

## 7.3 每次 Bloom 范围

初始建议：

```text
BLOOM_RADIUS_MIN = 0.9
BLOOM_RADIUS_MAX = 1.6

FLOWERS_PER_BLOOM_MIN = 28
FLOWERS_PER_BLOOM_MAX = 55
```

## 7.4 Bloom 内部结构

每个 Bloom：

```text
3–6 个内部 lobe / subcluster
```

参数：

```text
BLOOM_LOBE_MIN
BLOOM_LOBE_MAX
LOBE_OFFSET_RADIUS
```

## 7.5 生长动画

不要简单：

```text
0 → 1
```

而是：

```text
0
→ 0.7
→ 1.08
→ 1.0
```

并且：

```text
position.y = finalY - smallOffset
→ finalY
```

目标：

> 从地面迅速“膨出来”。

## 7.6 Bloom 时间节奏

建议：

```text
中心花：0–80ms
中层：70–180ms
外围：150–320ms
总完成：500–900ms
```

---

# 8. 花数量上限问题

后来出现：

> 长到一定数量后，再拖鼠标没有反应。

原因：

最初 Demo 为防止浏览器崩溃，设置了：

```text
MAX_FLOWERS ≈ 3000
```

达到容量后停止生成。

后续建议提升到：

```text
MAX_FLOWERS ≈ 15000–25000
```

建议起点：

```text
MAX_FLOWERS = 20000
```

如果使用 InstancedMesh，需要注意：

```javascript
new THREE.InstancedMesh(geometry, material, capacity)
```

capacity 是固定的，必须重新创建足够大的实例容量。

---

# 9. 3D 紫金草模型阶段

## 9.1 推荐格式

如果用模型：

```text
zijincao.glb
```

要求：

- 已有材质
- 已有 UV
- Pivot / Origin 在根茎底部
- Scale = 1,1,1
- 面数尽量低
- web-ready / game-ready

## 9.2 Pivot 重要性

正确：

```text
        🌸
        │
        │
────────●────────
       Origin
```

错误：

```text
        🌸
        │
        ●
        │
──────────────
```

因为 Bloom 生长时：

```text
scale 0 → 1
```

要从地面向上长。

## 9.3 Blender 中文版约定

之后 Blender 教程：

- 快捷键可写英文：
  - `G`
  - `S`
  - `R`
  - `Tab`
  - `Shift + S`
  - `Ctrl + A`
- 所有菜单 / 面板 / 属性都用中文版名称

例如：

- “材质属性”
- “设置原点 → 原点到 3D 游标”
- “表面细分”
- “材质预览”

## 9.4 Blender 检查 Pivot

流程：

1. 选中模型
2. 打开“视图叠加层 → 原点”
3. 看橙色小点是否在根茎底端
4. 如果不在：
   - `Tab` 进入编辑模式
   - 选根茎最底部顶点
   - `Shift + S`
   - 选“游标到选中项”
   - `Tab` 回物体模式
   - 右键
   - “设置原点 → 原点到 3D 游标”

---

# 10. 为什么最终放弃 3D 模型为主

实际发现：

- 正常好看的 AI 3D 花模型通常需要 5000+ 面
- 大量复制后性能压力太大
- 目标是整屏花田
- 还要叠加交互和后续粒子效果

因此最终决定：

> **PNG Billboard / Flower Card 为主**

模型版保留作为备份。

---

# 11. PNG 花卡方案

## 11.1 原理

```text
透明背景紫金草 PNG
+
PlaneGeometry
+
透明材质
+
InstancedMesh
=
2.5D 花丛
```

优势：

- 每株只有 2 个三角面左右
- 可以上万株
- 适合 BloomEvent
- 适合后续粒子化
- 比 GLB 稳定得多

## 11.2 PNG 素材规范

最终建议：

```text
尺寸：约 512×1024
格式：PNG
背景：真实透明 Alpha
内容：单株紫金草
主茎：大致竖直
根茎底部：接近图片底边
底部透明边距：约 2%–4%
左右边缘：约 5%–8%
根部：大致水平居中
不要地面阴影
不要白边 halo
不要大量透明空白
```

## 11.3 变体

最终准备了 5 个 PNG 变体：

```text
zijincao_01.png
zijincao_02.png
zijincao_03.png
zijincao_04.png
zijincao_05.png
```

放在：

```text
public/assets/flowers/png/
```

## 11.4 PNG Card 朝向问题

不能像 3D 模型那样：

```text
rotationY = random(0–360°)
```

否则平面可能侧过来变成一条线。

正确方法：

- 基本面向摄像机
- 在此基础上只做轻微 yaw：

```text
±15°～25°
```

## 11.5 Bottom Anchored Geometry

Plane 原点需要在底部中心。

例如：

```javascript
const geometry = new THREE.PlaneGeometry(width, height)
geometry.translate(0, height / 2, 0)
```

这样：

```text
local (0,0,0)
=
植物根部
```

Bloom 时就能从地面生长。

---

# 12. 同时保留模型版与 PNG 版

最终要求：

```text
http://127.0.0.1:5173/model.html
```

= 保留 GLB 模型版本

```text
http://127.0.0.1:5173/png.html
```

= 主要 PNG 花卡版本

推荐结构：

```text
project/

├── model.html
├── png.html

├── src/
│   ├── model-main.js
│   ├── png-main.js
│
│   ├── shared/
│   │   ├── PointerController.js
│   │   ├── MouseInput.js
│   │   ├── GroundRaycaster.js
│   │   ├── BloomSystem.js
│   │   ├── createScene.js
│   │   └── createGrass.js
│
│   ├── model/
│   │   └── ModelFlowerRenderer.js
│
│   └── png/
│       └── PNGFlowerRenderer.js

└── public/
    └── assets/
        └── flowers/
            ├── zijincao.glb
            └── png/
                ├── zijincao_01.png
                ├── zijincao_02.png
                ├── zijincao_03.png
                ├── zijincao_04.png
                └── zijincao_05.png
```

---

# 13. 草地方案

草地没有另外准备模型。

决定：

> 草由代码程序化生成。

方案：

- Ground Plane
- 低模草叶
- InstancedMesh
- 高度随机
- 旋转随机
- 轻微 sway

草只是背景，不抢花。

---

# 14. PNG 花密度问题

后来出现：

> 花密度太大，看不到草，有点恶心。

调整原则：

- 不推翻 Bloom
- 减少每个 Bloom 内花数量
- 保留花丛感
- 草地要有负空间

建议：

```text
FLOWERS_PER_BLOOM_MIN / MAX
降低约 20%–35%
```

必要时略减：

```text
FLOWER_CARD_HEIGHT
FLOWER_SCALE_MAX
```

---

# 15. 页面视觉氛围演化

## 15.1 第一版

白天 / 浅绿色草地：

- 偏植物观察
- 偏自然
- 不够神秘

## 15.2 目标

参考 P2：

- 黑色 / 深紫背景
- 神秘
- 夜晚
- 上方紫色光
- 记忆空间
- 展览感

## 15.3 第一次暗色化

改成：

- 黑紫背景
- 上方紫光
- 深色草地
- 花更突出

但问题：

- 太黑
- 背景像黑幕
- 花和环境割裂
- 草地被吃掉

## 15.4 第二次色调校准

目标：

> **深紫夜色 + 中等亮度雾感背景 + 柔和紫光 + 蓝紫灰绿色调**

要求：

- 背景不要死黑
- 有中间调
- 地平线更柔
- 草地灰绿
- 花稍微降“糖果感”
- UI 不用纯白

---

# 16. 草地边界问题

后来发现：

> 草地边缘太固定，看起来像矩形区域。

优化方向：

- 弱化 Ground 边缘
- 远处草密度不规则下降
- 距离 fog
- 草地与背景渐隐
- 地平线柔化
- 不让观众看出明确平台边界

目标：

> 场景像延伸进黑紫色记忆空间，而不是一块被裁出来的平面。

---

# 17. 页面标题 UI

原来：

```text
紫金草
```

改为：

```text
INTERACTIVE MEMORY GARDEN
记忆花园
MEMORY BLOOMS
```

位置：

- 顶部居中
- `MOUSE INPUT · READY` 保留右上角

---

# 18. Memory Entry / 留言弹窗

进入 `/png.html`：

先显示中央留言弹窗。

文案：

## 标题

**留下一段你想保存的记忆**

## 描述

```text
关于南京大屠杀、江东门，
或你与这个地点产生联系的某个瞬间。
```

## 输入提示

```text
可以是一句话、一个地点、
一种声音，或第一次知道这段历史的时刻。
```

## 按钮

**将记忆留在花园中**

## 18.1 提交前

```text
MOUSE INPUT · WAITING
```

种花禁用。

## 18.2 提交后

- 弹窗淡出
- 背景 blur 减少
- 用户记忆进入 session memoryPool
- 自动触发第一丛花
- 显示用户自己的文字：

```text
YOUR MEMORY
```

然后：

```text
MOUSE INPUT · READY
```

---

# 19. Memory Echo / 记忆浮层

## 19.1 原来的问题

原卡片：

- 实心深色矩形
- 像调试面板
- 太突兀
- 和花、水彩、紫色氛围不统一

## 19.2 最终视觉方向

参考第三张 P 图：

> 漂浮在花园中的“记忆碎片”

样式：

```text
半透明深紫黑玻璃
+
backdrop blur
+
淡紫边框
+
非常轻的紫色 glow
+
浅灰紫文字
```

建议：

```css
background: rgba(18,12,28,0.45~0.62);
backdrop-filter: blur(16px~24px);
border: 1px solid rgba(180,145,255,0.18~0.32);
border-radius: 8px~12px;
```

不要：

- Bootstrap Modal
- 社交媒体评论框
- 大圆角 App 卡片
- 赛博霓虹

## 19.3 Memory Card 信息

例如：

```text
“那天没有说很多话，
只记得离开前又回头看了一次。”

MEMORY · 021
```

## 19.4 动画

进入：

```text
opacity 0 → 1
translateY 8px → 0
scale 0.97 → 1
350–550ms
```

停留：

```text
3.5–5s
```

退出：

```text
opacity 1 → 0
translateY 0 → -8px
500–800ms
```

---

# 20. Memory Echo 触发频率

原问题：

> 拖很长一段才能出现一个记忆。

最终改为：

> **一次完整拖拽 gesture 最多出现 2–3 条 Memory Echo。**

Gesture：

```text
pointer down
↓
drag
↓
pointer up
```

## 20.1 频率逻辑

短拖：

```text
1 条
```

中等：

```text
2 条
```

长拖：

```text
3 条
```

推荐：

```text
MAX_MEMORIES_PER_GESTURE = 3
MAX_ACTIVE_MEMORY_CARDS = 3
```

可以根据：

- BloomEvent 数量
- 世界距离
- 两者混合

触发。

例如：

```text
每 2–4 个 BloomEvent
→ 出现一个 Memory Echo
```

## 20.2 Memory Card 布局

卡片：

- 贴近触发它的 BloomEvent
- 世界坐标投影到屏幕坐标
- 尝试：
  - upper-left
  - upper-right
  - left
  - right
- 避免重叠
- 保持 viewport 内
- `pointer-events: none`

---

# 21. 记忆内容来源

Demo 阶段：

```text
src/data/memoryPool.js
```

本地数组即可。

用户提交内容：

```text
push 到 session memory pool
```

重要原则：

> 不要让 Codex 编造“真实幸存者证言”并假装是真实引用。

如果是 placeholder：

- 标明 prototype / sample
- 或使用自己收集到的匿名留言

---

# 22. “一朵花 = 一段记忆”的未来数据关系

未来可扩展：

```javascript
BloomEvent {
  position,
  memoryId,
  ...
}
```

最终：

```text
🌸 → Memory 014
🌸 → Memory 027
🌸 → Memory 105
```

甚至未来：

- hover 花朵
- 再次查看记忆

Demo 暂时不做。

---

# 23. 粒子化数字效果计划

这部分尚未正式实现。

最终构想：

```text
BloomEvent
↓
Particle Seed
↓
地面出现紫色数字颗粒
↓
粒子向上 / 向外膨胀
↓
粒子向花的目标位置聚合
↓
PNG 紫金草逐渐显现
↓
粒子淡出并回收
↓
留下完整花丛
```

## 23.1 建议时间

```text
0–100ms
地面出现数字颗粒

100–300ms
粒子向上 / 向外膨胀

250–500ms
粒子向花区域聚合

350–700ms
PNG 花丛长出来

450–800ms
粒子淡出

800–1000ms
花丛稳定
```

## 23.2 性能原则

不要：

```text
每一株花永久拥有 100 个粒子
```

否则：

```text
20000 × 100
= 2,000,000 particles
```

正确：

> 粒子只存在于 Bloom 动画阶段。

使用：

```text
Particle Pool
```

复用。

---

# 24. 当前网页主要版本状态

主版本：

```text
/png.html
```

特点：

- PNG 花卡
- 5 个紫金草变体
- 深紫黑记忆花园氛围
- 程序化草
- BloomEvent
- Memory Entry Modal
- Memory Echo UI
- 鼠标交互

保留版本：

```text
/model.html
```

= GLB 模型版

要求：

> 后续修改 `/png.html` 时不要破坏 `/model.html`。

---

# 25. Codex 项目迁移 / 换电脑

推荐：

> Git + Private GitHub

项目根目录应包含：

```text
AGENTS.md
README.md
png.html
model.html
src/
public/
package.json
package-lock.json
```

不要上传：

```text
node_modules/
dist/
```

---

# 26. AGENTS.md 应包含

- 项目名
- 技术栈
- 两个入口 URL
- 当前视觉方向
- 当前交互
- BloomEvent 规则
- PNG 花卡规则
- Memory Echo 规则
- 未来粒子计划
- 未来 MediaPipe
- asset path
- config path
- Skills 列表
- TODO
- 当前已知问题

---

# 27. 换电脑后的 Codex 启动 Prompt

新机器 Codex：

```text
Read AGENTS.md and README.md first.

Inspect the current repository before making any changes.

This is a continuation of an existing project.
Do not rebuild or redesign anything from scratch.

After reading the documentation, summarize:
1. the current project architecture
2. what /png.html and /model.html are
3. the current interaction flow
4. the next planned task

Do not edit files yet.
```

---

# 28. 同一台电脑换 Codex 账号

本地项目文件不会因为换账号消失。

建议流程：

1. 当前账号 Codex：
   - 更新 AGENTS.md
   - 更新 README.md
   - Git checkpoint
2. 登出
3. 新账号登录
4. 打开同一项目目录
5. 新 Codex 先读：
   - AGENTS.md
   - README.md
6. 检查 Skills
7. 再继续开发

---

# 29. 当前账号 Codex 交接 Prompt 核心要求

当前 Codex 要做：

- 检查完整项目
- 确认 `/png.html`
- 确认 `/model.html`
- 更新 AGENTS.md
- 更新 README.md
- 检查 `.gitignore`
- `git status`
- checkpoint commit：

```text
checkpoint: memory garden before account switch
```

- 记录 Skill 名
- 报告：
  - project root
  - branch
  - commit hash
  - working tree
  - asset 是否完整

---

# 30. 新账号 Codex 第一次要说的话

```text
This is an existing project that was developed in another Codex account on this same computer.

Do NOT rebuild it from scratch.

The existing local project files are the source of truth.

First:
1. Read AGENTS.md completely.
2. Read README.md.
3. Inspect the current repository structure.
4. Inspect git status and latest commit.
5. Inspect the existing code before making edits.

Do NOT modify files yet.

Then summarize:
- project concept
- png.html
- model.html
- current visual direction
- interaction flow
- BloomEvent
- PNG rendering
- Memory Entry Modal
- Memory Echo
- current TODO
- missing Skills
```

---

# 31. 后续开发固定原则

以后每次修改：

1. 不重做现有项目
2. `/model.html` 默认保持不变
3. `/png.html` 为主版本
4. Bloom 不能回到线性轨迹
5. PNG 花必须从根部生长
6. Memory Echo 不能挡鼠标
7. 视觉保持：
   - 深紫
   - 神秘
   - 安静
   - 记忆感
   - 避免 cyberpunk
8. 后续 Prompt 必须同时列出：
   - 本次需要启用的 Skills

---

# 32. 当前下一步建议

优先级：

## P0 — Demo 稳定

- 确认留言 UI 好看
- 确认一拖出现 1–3 条 Memory Echo
- 确认 Memory Card 不重叠 / 不出屏
- 确认 `/model.html` 仍然正常
- Git checkpoint

## P1 — 粒子 Bloom

实现：

```text
地面颗粒
→ 膨胀
→ 聚合
→ PNG 花显现
→ 粒子淡出
```

## P2 — 手势输入

接入：

```text
MediaPipe Hand Landmarker
```

把：

```text
MouseInput
```

替换为：

```text
HandInput
```

保留鼠标 fallback。

---

# 33. 一句话项目总结

> **《记忆花园》是一个以紫金草为视觉媒介的交互网页。观众首先留下与南京大屠杀、江东门或个人地点经验相关的一段记忆，随后通过鼠标（未来为手势）在草地上唤起一丛丛紫金草。每一次花的生长都对应着一段被保存、被重新唤起的个人记忆，最终由无数记忆共同组成一座数字花园。**

---

# 34. 当前最终技术概览

```text
Vite
Vanilla JavaScript
Three.js

Main:
png.html

Backup:
model.html

Input:
MouseInput
→ Future MediaPipe HandInput

Interaction:
PointerController
↓
GroundRaycaster
↓
BloomEvent
↓
PNGFlowerRenderer
↓
Memory Echo

Assets:
5 × transparent Zijincao PNG

Grass:
procedural / instanced

Atmosphere:
dark violet memory garden

Memory:
local prototype memoryPool
no backend yet

Next:
Particle Bloom
→ Hand Tracking
```

---

# 35. 文件与路径建议

```text
project/
├── AGENTS.md
├── README.md
├── model.html
├── png.html
├── package.json
├── package-lock.json
├── src/
│   ├── model-main.js
│   ├── png-main.js
│   ├── shared/
│   ├── model/
│   ├── png/
│   └── data/
│       └── memoryPool.js
└── public/
    └── assets/
        └── flowers/
            ├── zijincao.glb
            └── png/
                ├── zijincao_01.png
                ├── zijincao_02.png
                ├── zijincao_03.png
                ├── zijincao_04.png
                └── zijincao_05.png
```

---

# 36. 最重要的视觉关键词

```text
Memory
Garden
Zijincao
Lavender
Dark violet
Frosted glass
Soft glow
Bloom
Memory Echo
Particle
Mist
Dreamlike
Quiet
Poetic
Exhibition
```

避免：

```text
Cyberpunk
Neon
Sci-fi UI
Bright daytime field
Hard rectangular cards
Linear flower trail
Debug-panel look
```

---

# 37. Demo 展示时建议的操作顺序

1. 刷新 `/png.html`
2. 展示留言弹窗
3. 输入一段简短记忆
4. 提交
5. 展示第一丛自动 Bloom
6. 展示自己的记忆浮层
7. 按住鼠标开始拖动
8. 展示 2–3 条 Memory Echo
9. 让花丛继续生长
10. 展示整个“由记忆组成的花园”
11. 如果需要，展示 `/model.html` 作为模型技术备份

---

# 38. 当前最重要的开发提醒

- 不要为了“更高级”重新引入高模 3D 花
- PNG 方案是主方案
- 不要让花密度再次过高
- 草地边缘继续保持弱化
- Memory UI 要和花的紫色视觉统一
- UI 应像漂浮的“记忆碎片”
- 不是普通 Modal
- 不要用真实历史证言时编造内容
- Demo 优先稳定
- 粒子和手势是下一阶段
